# 整改清单 (Remediation Checklist) — [Iteration]

> **事实来源 #1**：本文件是 10 项整改问题（R-001~R-010）的绝对真理。
>
> **源计划**：`docs/整改清单_修复优先级_验证用例表.md`
>
> **状态生命周期**：新建 → 已确认 → 修复中 → 待复核 → 已关闭（可暂缓/拒绝）

## 1. 整改问题清单

| 编号 | 优先级 | 模块 | 问题描述 | 影响 | 建议整改内容 | 状态 | 发现日期 |
|---|---|---|---|---|---|---|---|
| R-001 | P0 | 前端路由 | chunk 加载失败处理将 `SyntaxError` 一并视为 chunk 失效，可能误刷真实语法错误 | 中高 | 收窄错误判定条件，仅对明确的动态导入失败/ChunkLoadError 执行自动刷新；其余情况上报并展示错误页 | 新建 | YYYY-MM-DD |
| R-002 | P1 | 前端请求 | 登录跳转兜底仅使用 `window.location.pathname`，会丢失 query/hash | 中 | 保存 `pathname + search + hash` 或统一使用 `fullPath`，保证登录后可恢复原上下文 | 新建 | YYYY-MM-DD |
| R-003 | P2 | 前端请求 | `ElMessage` 依赖自动导入，基础文件隐式耦合 | 中高 | 在请求层、路由守卫层优先采用显式导入或补充构建验证，降低自动导入失效风险 | 新建 | YYYY-MM-DD |
| R-004 | P2 | 前端请求 | GET 去重 key 依赖 `JSON.stringify`，复杂参数存在歧义风险 | 中低 | 为复杂参数引入稳定序列化方案；当前仅保证平面参数场景稳定 | 新建 | YYYY-MM-DD |
| R-005 | P1 | 后端预测 | fire-and-forget 任务失败仅记录日志，可观测性不足 | 中 | 增加后台任务成功/失败/超时指标，必要时引入持久化任务记录 | 新建 | YYYY-MM-DD |
| R-006 | P1 | 后端启动 | 多个后台组件启动失败仅日志记录，缺少结构化降级原因 | 中 | 在 health snapshot 或启动状态中增加组件失败原因摘要，并联动告警 | 新建 | YYYY-MM-DD |
| R-007 | P1 | 前端性能 | 图表页与 ECharts 相关资源仍偏重，首屏体积可继续优化 | 中高 | 图表页按需加载、延迟加载 ECharts 核心与扩展模块，减少主包体积 | 新建 | YYYY-MM-DD |
| R-008 | P0 | 前端性能 | `element-plus` 体积较大，需进一步核对按需引入效果 | 高 | 审计是否存在全量引入或重复打包，确保自动导入与按需加载完全生效 | 新建 | YYYY-MM-DD |
| R-009 | P2 | 前端性能 | 页面初始渲染存在重计算、图表 resize、列表密集渲染等潜在卡顿点 | 中高 | 使用分页/虚拟列表/节流 resize/缓存计算结果，降低主线程阻塞 | 新建 | YYYY-MM-DD |
| R-010 | P0 | 功能验证 | 关键业务链路仍缺少完整端到端实测结论 | 高 | 补充登录、刷新、权限、预测、复核、上传、告警、离线等 E2E 用例并执行 | 新建 | YYYY-MM-DD |

## 2. 修复优先级清单

### P0：必须优先修复

| 优先级编号 | 关联问题 | 项目 | 原因 | 目标完成标准 | 状态 |
|---|---|---|---|---|---|
| P0-1 | R-008 | `element-plus` 按需引入审计 | 直接影响首屏体积和整体包大小，收益高 | 确认无全量引入；构建产物中 `element-plus` chunk 体积显著下降 | 新建 |
| P0-2 | R-010 | 关键链路 E2E 实测 | 当前"功能可用"主要来自静态审查，需运行验证闭环 | 至少覆盖登录、刷新、权限、预测、复核 5 条主链路 | 新建 |
| P0-3 | R-001 | chunk 失败误判修正 | 可能掩盖真实运行时错误，影响排障 | 仅在明确 chunk 失效时自动刷新，真实语法错误不被吞掉 | 新建 |

### P1：高优先级修复

| 优先级编号 | 关联问题 | 项目 | 原因 | 目标完成标准 | 状态 |
|---|---|---|---|---|---|
| P1-1 | R-007 | 图表页与 ECharts 懒加载 | 对首屏和交互性能有直接收益 | 图表类页面进入后再加载 ECharts 相关模块 | 新建 |
| P1-2 | R-002 | 登录跳转保留完整 URL | 影响复杂页面恢复体验 | 登录后可恢复 query/hash 状态 | 新建 |
| P1-3 | R-005 | fire-and-forget 任务可观测性 | 影响后台任务可靠性定位 | 可以统计调度成功率、失败率、超时率 | 新建 |
| P1-4 | R-006 | 启动失败结构化状态 | 影响健康检查与运维排障 | health 或日志中能明确定位失败组件 | 新建 |

### P2：中优先级修复

| 优先级编号 | 关联问题 | 项目 | 原因 | 目标完成标准 | 状态 |
|---|---|---|---|---|---|
| P2-1 | R-003 | 显式导入或强化自动导入验证 | 提升基础层稳定性和可移植性 | 关键基础文件不依赖隐式注入或已补齐验证 | 新建 |
| P2-2 | R-004 | 稳定序列化 GET 去重 key | 目前风险较低，但复杂参数场景存在扩展隐患 | 支持复杂参数的稳定 key 生成 | 新建 |
| P2-3 | R-009 | 页面重计算与 resize 节流优化 | 改善中低端设备交互体验 | 大列表、大图表页面滚动和缩放更平滑 | 新建 |

## 3. 建议执行顺序

1. 先修复 P0 项：`R-001`、`R-008`、`R-010`
2. 再修复 P1 项：`R-002`、`R-005`、`R-006`、`R-007`
3. 最后处理 P2 项：`R-003`、`R-004`、`R-009`
4. 每个修复项完成后，至少执行对应验证用例中的 1~2 个核心场景
5. 所有 P0/P1 用例完成后，再做一次完整回归

## 4. 状态图例

- 🆕 新建
- ✅ 已确认
- 🔧 修复中
- ⏳ 待复核
- ✔️ 已关闭
- ⏸️ 暂缓
- ❌ 拒绝
