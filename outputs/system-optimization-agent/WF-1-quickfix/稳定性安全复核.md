# WF-1 快速止血 — 稳定性与安全性复核结论

> 阶段：WF-1 | 技能：sys-reliability / sys-security-hardening | 日期：2026-07-14
> 核心原则：**先数据后决策**。本章对 WF-0 问题清单中「稳定性/安全」类项逐条代码复核，
> 发现本工程（毕业设计级，已多轮优化）对已规划手段**大部分已实现**，故以「证据纠正」替代「盲改」。

## 1. 逐条复核结果

| 问题 | WF-0 初判 | 代码复核结论 | 证据 |
|---|---|---|---|
| ISS-01 N+1 | P1 高风险 | **误报，降级 P3** | 7 个 relationship 在应用层 0 处遍历；`get_reviews` 仅序列化标量列 |
| ISS-09 限流/熔断/超时 | P1 不完整 | **已实现，降级 P3** | `core/rate_limit.py` 全局默认 60/min + auth/gdpr 严格限流；`db_breaker.py`/`celery_breaker.py` + `predict.py` 用 `CircuitBreakerOpenError`；全出站调用均含 `timeout` |
| ISS-07 漏洞扫描 | P0 待补 | **bandit 已跑** | 见 §2 |
| ISS-05 压测基线 | P1 pending | **脚手架已存在** | `backend/load_tests/locustfile.py` 含场景与基线目标（见 §3）|

## 2. 安全基线（bandit SAST，实测）

命令：`.venv/Scripts/bandit.exe -r app -x "*/tests/*,*/test_*"`

| 指标 | 值 |
|---|---|
| 扫描代码行数 | 45,180 |
| **High** | **0** |
| Medium | 11（含 2× B615 HuggingFace `from_pretrained` 未固定 revision）|
| Low | 19 |

**结论**：SAST 层面无高危项；符合计划 §3「高危漏洞 24–72h 清零」的当前状态（0 高危）。
新增 **ISS-13**（B615 未 pin revision，P2）作为 WF-2 加固项。
> 说明：本环境未安装 trivy，依赖漏洞扫描（pip-audit/safety）尚未执行，已记入 `KPI-基线.json` 的 `trivy_high_critical=pending`。

## 3. 压测基线脚手架（已存在，运行期数据待采）

`backend/load_tests/locustfile.py` 已覆盖匿名探针 / 管理员 / 普通用户 / 模型推理四类场景，
并预定义了基线目标（如 `/health/live` P99<30ms >1000RPS、`/model/predict/fusion` P99<1200ms）。
**缺失的仅是「把这套脚本跑起来、把数字写回 KPI-基线.json」**。标准执行：

```bash
cd backend
# 测试模式启动（无 Redis 依赖，限流器降级内存存储）
APP_ENV=test DATABASE_URL=sqlite+aiosqlite:///./test_app.db \
  .venv/Scripts/python.exe -m uvicorn app.main:app --port 8000 &
# 无头压测（需在 seed DB 后）
.venv/Scripts/python.exe -m locust -f load_tests/locustfile.py \
  --host=http://localhost:8000 --headless -u 50 -r 10 -t 60s --only-summary
```

## 4. Gate-1 判定（稳定性/安全部分）

- 限流/熔断/超时：**已落地并通过代码复核** ✅
- 安全 SAST：**0 High** ✅（trivy/依赖扫描 pending，非代码阻断项）
- 运行期压测基线：**脚手架就绪，数字待采**（需可启动的后端 + seed DB）
- 前端首屏（上一份报告）：代码优化已落地 + typecheck/单测通过，Lighthouse 分数待 Chrome 环境复测

## 5. 修正后的剩余行动建议

1. 在有 Chrome 的环境复测前端 Lighthouse（见上份报告命令）。
2. 启动后端并跑 locust，将 P95/P99/QPS 写回 `KPI-基线.json`（ISS-05）。
3. 执行 trivy + pip-audit 补齐依赖漏洞扫描（ISS-07 收尾）。
4. WF-2：ISS-13 固定 HuggingFace 模型 revision；按需深化（本项目性能/稳定性手段已较完备）。
