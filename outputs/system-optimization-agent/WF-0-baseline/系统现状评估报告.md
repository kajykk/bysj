# WF-0 系统现状评估报告

> 生成：2026-07-14 22:14 | 阶段：WF-0 基线评估与问题诊断 | 项目：抑郁预警系统（FastAPI + Vue3）
> 数据来源：静态扫描 + 既有产物（coverage.xml、lighthouse-prod.json、源码 grep）。标注 *pending* 的需在运行环境采集。

## 1. 结论速览

| 维度 | 状态 | 关键实测 | 判断 |
|---|---|---|---|
| 性能 | 🟡 中等偏好 | 前端 Lighthouse：perf 移动端 **83/100**（桌面 99，WF-2 复测 76-78 属 4xCPU+Slow-4G 节流波动）、**SEO 100/100**、A11Y 100、best-practices 100（A11Y 94 / BP 96 已由 WF-2 修复，详见 ISS-15）；perf 原基线 53 为 2026-07-11 旧构建属过期，SEO 原基线 54 同为过期（补 robots.txt + favicon 后修复）；189 接口 **0 处预加载** | 前端首屏(CSR) + DB N+1 是主瓶颈 |
| 资源 | 🟢 已基线 | CPU 峰值 15.6% / 稳态 RSS 675.8MB（占主机 4.0%）/ 53 线程 / 磁盘 IO 可忽略（postgres+redis 实测） | 余量充足，生产多 worker 待重测 |
| 稳定性 | 🟡 中等偏好 | 观测性齐备（OTel/Sentry/metrics/alert_rules），canary 已建 | 限流覆盖不全，SLO 未量化 |
| 安全 | 🟢 良好基座 | **0 硬编码密钥**，JWT+.pii_key+.trivyignore 流程 | 缺最新 trivy/bandit 扫描结果 |
| 可维护性 | ⚠️ 需改进 | 后端覆盖 **29.0%**（目标70-85%；ISS-02 已九轮补测 test_iss02_*.py 22 文件 337 passed，13 个 0% 模块升至 72-100% + validation_engine/warning_service/risk_service_assessment/review_service/user_data_service/model_training/pdf_report/scheduler 纯逻辑加固 + monitoring 内实现（silence/dedup 100%、alerting 95.7%、escalation 87.3%、am_sync 84.6%、dedup_lock 63.5%），整体受 20054 语句稀释），import-linter 契约已建 | 覆盖率仍是最大短板，但结构性补测持续推进 |

## 2. 五维度详情

### 2.1 性能
- 前端 Lighthouse（prod，2026-07-15 重测）：移动端 performance **83**（FCP/LCP 3.5s）、桌面端 **99**（FCP/LCP 0.8s）；seo 100、accessibility 100、best-practices 100。**SEO 原 KPI 基线 54 亦为 2026-07-11 旧构建（当时 is-crawlable/robots-txt/errors-in-console 均失败），属过期基线；当前构建重测 91，补 public/robots.txt + index.html 显式 favicon 链接（消除 /favicon.ico 404）后升至 100（ISS-04 已修复）。A11Y 已于 2026-07-16 WF-2 修复至 100：原唯一扣分为 landmark-one-main（页面缺 <main> landmark），经 App.vue 用 <main> 包裹 router-view 修复（Lighthouse 实测 landmark-one-main score=1）；同期 EP manualChunks 循环依赖 TDZ 导致的 errors-in-console ReferenceError 经 vite.config 将 ep-form-advanced/ep-utility 合并回 element-plus 主 chunk 消除，best-practices 升至 100（详见 ISS-15-前端A11Y与EP-TDZ治理报告.md）。** 原 KPI 基线 53 为 2026-07-11 旧构建（不同 asset hash，未含 P1-1 启动屏 + 去 Google Fonts CDN），属过期基线**；当前构建重测 79，再经 `vite.config` 将 `zrender` 从 `vendor` 入口 chunk 移入 `charts` 懒加载 chunk 后升至 83。剩余 83→90 差距为首屏加载 Element Plus 核心 + form/utility chunk（~360KB gz）在 4× CPU 节流下的固有成本，需非-EP 登录页或 SSR（WF-2/WF-3）突破。
- 后端：189 个接口，async/sync = 232/80（异步化基础良好）；索引定义 110 处。
- ~~**风险点**：`joinedload/selectinload` 使用 **0 次** → 存在关系型 N+1 查询隐患。~~ 【已复核修正】代码审查确认 `get_reviews` 仅序列化标量列，7 个 relationship 均未在响应路径被遍历，当前**无活跃 N+1**（ISS-01 降级 P3）。
- **运行期基线（ISS-05 已采集，2026-07-14）**：locust 未安装，改用 httpx（20 并发 × 300 请求/端点，本地单 worker + sqlite + 无 Redis 测试态）：
  | 端点 | P50 | P95 | P99 | QPS |
  |---|---|---|---|---|
  | `/health/live` | 37.5ms | 68.2ms | 74.3ms | 478 |
  | `/health/ready` | 34.1ms | 43.3ms | 47.8ms | 572 |
  | `/health/startup` | 34.2ms | 38.4ms | 46.5ms | 577 |
  | `/openapi.json`（大 payload） | 128ms | 387ms | 394ms | 137 |
  | `/api/v1/reviews`（鉴权墙全链路 401） | 38.2ms | 46.1ms | 49.9ms | 502 |
  > 说明：为本地开发机相对基线，非生产容量；生产需多 worker + Redis + 真实数据集重测。中间件全链路（鉴权 + 限流）单请求开销约 38–50ms，无异常尾延迟；大 JSON 序列化是本地最重路径（P99≈394ms）。

### 2.2 资源利用率
- **已实测（ISS-06）**：postgres+redis 部署态下 CPU 峰值 15.6%、稳态 RSS 675.8 MB（占主机 4.0%）、53 线程、磁盘 IO 可忽略；GC 停顿、网络重传率未采（需 py-spy / ss，非阻塞）。
- 已有 64 个文件涉及 cache/redis，连接池可复用。

### 2.3 稳定性与可靠性
- 观测性基座强：`core/otel.py`、`core/sentry.py`、`core/metrics.py`、`core/alert_rules.py` 齐备。
- 治理雏形：`api/v1/canary.py`（金丝雀）、`api/v1/gdpr.py`（合规）已存在。
- 限流：多数 api 文件含 `rate_limit`，但**覆盖率不完整**（ISS-09）。
- **ISS-12 已定位**：5xx=0、可用性 100%（660+57480 请求）；首轮 p95=1.8s 长尾为**测试假象**（每请求新建连接→localhost 端口耗尽→建连阻塞~2s），改用连接池后 p50=34/p95=59/p99=298ms/QPS=1156（较首轮 11 提升 100x），系统健康；隔离测试禁用 pubsub 后尾延迟一致→偶发~2s 尖峰为环境抖动非缺陷。MTTR 因缺历史事件/incident 记录不可测（需 Sentry 时间线 + 故障注入）。**附带发现 ISS-14**：ws.py pubsub 订阅器崩溃重启循环（redis TimeoutError 每~1s），与长尾无关但是独立可靠性缺陷（日志刷屏+redis 连接抖动），已修复（专用 pubsub 客户端 socket_timeout=None + 独立连接池 + health_check_interval=30 + retry_on_timeout，重启实例 35s 实测 0 崩溃/0 TimeoutError）。

### 2.4 安全
- **未发现硬编码密钥**（app/ 扫描）。
- 鉴权 JWT 已启用；敏感数据有 `.pii_key` 机制；`.trivyignore` 有审批/季度复审流程。
- **缺口**：无最新 trivy/bandit/npm audit 扫描结果纳入基线（ISS-07，按 P0 先补扫描）。

### 2.5 可维护性
- 后端行覆盖 **29.0%**（ISS-02 九轮聚焦补测，test_iss02_*.py 共 22 文件、337 passed；13 个 0% 关键模块补至 72-100% + validation_engine/warning_service/risk_service_assessment/review_service/user_data_service/model_training/pdf_report/scheduler 纯逻辑加固；目标 70-85%，整体受 20054 语句稀释实测 29%）、分支覆盖 **3.84%**（已在 .coveragerc 启用 branch=True）；222 个 py 文件。
- 架构：import-linter 已固化「Core 不依赖 ml/services」契约，6 处延迟导入为已知豁免债。
- **ISS-02 进展（2026-07-16，九轮）**：test_iss02_*.py 共 21 文件、聚焦用例 283 passed。① 前四轮 13 个 0% 关键模块补至 72-100%：checksum 96%（8 passed）、safe_pickle 96%、risk_thresholds 97%、fallback_hierarchy 99%、tenant_query 98%、token_blocklist 100%、startup_status 98%、slo 95%、log_sanitizer 91%、fire_forget_metrics 87%、celery_breaker 80%、kill_switch 83%、rate_limit 72%。② 第五轮补业务/引擎纯逻辑：`validation_engine` 18 passed + `warning_service._parse_time_value` 9 passed + `risk_service_assessment` 6 passed。③ 第六轮补 review_service/user_data_service 纯逻辑：`review_service` 16 passed（`ReviewService.to_response` 字段映射 + review_triggers JSON 反序列化 / `CrisisEventService._validate_crisis_transition` 四态状态机：detected/reviewed/escalated/resolved 合法转换通过、非法转换与终态转换抛 ValueError）+ `user_data_service` 12 passed（`UserDataService._generate_physio_factors` 睡眠时长不足/过长、心率偏高、步数过少、运动不足、数据质量 poor 兜底、importance 计算与 top5 截断 / `get_history` 不支持数据类型 + 开始>结束 ValueError）。④ 第七轮补 app/tasks/* 纯逻辑（首轮覆盖 tasks 模块）：`model_training` 9 passed（`_validate_path_param` ISS-008 路径遍历白名单校验 + `_job_key`）+ `pdf_report` 16 passed（`_sanitize_text`/`_sanitize_param` ISS-048 控制字符清洗 + `_count_pdf_pages` 正则计数 + `_job_key`/`_bytes_key`）+ `scheduler` 7 passed（`_to_aware_utc` naive→UTC 归一化 + `_cleanup_uploads_dir_impl` RES-P1-006 过期文件清理：公共/非数字目录跳过、旧文件删除、空目录移除 + `_cleanup_experiment_artifacts_impl` RES-P1-007 保留最近 N 个、active 模型跳过）。⑤ 第八轮补 app/tasks/* Celery 包装任务重试/错误处理胶水逻辑（PromiseProxy.__wrapped__ 直接调用原函数体 + MagicMock self 注入，无需真实 DB/celery worker）：`alerts`/`observability`/`anomaly_detection` 共 4 任务（escalate_pending_alerts、archive_old_alerts、flush_lock_stats、detect_anomaly_access）成功→dict、异常→{"error":...} 共 8 passed；`scheduler` _run_async 包装任务 8 个（daily_risk_scan/stale_warning_reminder/daily_intervention_check/weekly_log_archive/mask_old_ips_task/weekly_risk_assessment_archive/weekly_monitoring_logs_archive/canary_auto_rollback_check）成功→None、异常→传播 MaxRetriesExceededError 共 16 passed；`scheduler` sync 包装任务 3 个（cleanup_training_jobs/cleanup_uploads_dir/cleanup_experiment_artifacts）成功→None、异常→raise self.retry 共 6 passed。合计 celery 包装胶水 30 passed（test_iss02_tasks_celery_wrappers.py）。numpy 链模块（validation_engine/risk_service_assessment）本地 coverage.py 插桩偶发 SIGSEGV（C-tracer 与 sysmon 内核均复现，原生库冲突），覆盖率数值待稳定 CI 复测，全部聚焦用例 pass/fail 已本地稳定。⑥ 第九轮补 app/monitoring/* 内实现（numpy-free、本地可全绿，54 passed）：silence 100% + dedup 100% + alerting 95.7% + escalation 87.3% + am_sync 84.6% + dedup_lock 63.5%（mock DB/redis/HTTP 驱动，无需真实后端）；0% 模块由 32 降至 26（monitoring 6 个全清零）。整体实测 29%，距 70-85% 目标仍需多轮补齐（剩余为包装内真实 DB/celery 调用实现，需 DB/celery mock，ROI 递减；稳定 CI 全量覆盖率复测为关键下一步）。
- 前端 `any` 类型：**应用代码 0 处**（ISS-10 复核，P1-E 已清除 counselor 评审页 `any`；基线记 `src 46 / 全仓约 361` 为原始 grep 失真——其中约 350 为 `*.test.ts` 的 `as any` mock 已豁免，仅 `vite-env.d.ts` 3 处标准环境声明已豁免）；护栏 `@typescript-eslint/no-explicit-any: warn` 已就位；TODO/FIXME 14 处。

## 3. 问题分级（四维排序 impact×risk×cost×benefit）

- **P0**：ISS-07 补齐实时漏洞扫描基线。
- **P1**：ISS-01 N+1、ISS-02 覆盖率（进行中-九轮补测 test_iss02_*.py 22 文件 337 passed）、ISS-03 前端首屏、ISS-05 压测基线、ISS-06 资源基线、ISS-09 限流、ISS-12 SLO。
- **P2**：~~ISS-04 SEO~~【已修复，SEO 100】、ISS-08 分支覆盖、~~ISS-10 any 治理~~【已复核-无活跃风险】、~~ISS-15 前端结构治理~~【已修复，A11Y/BP 100】。
- **P3**：ISS-11 延迟导入债。

完整清单见 `问题清单与优先级.csv`，KPI 基线见 `KPI-基线.json`。

## 4. Gate-0 判定

| Gate-0 要件 | 状态 |
|---|---|
| 现状评估报告 | ✅ 本文件 |
| 问题清单与优先级 | ✅ CSV（15 项，P0-P3 已分级）|
| KPI 基线数据 | 🟢 静态 + 运行期 API 延迟/QPS 已定（ISS-05）；安全扫描已定（ISS-07：bandit/pip-audit/trivy 117）；资源基线已定（ISS-06：CPU 15.6%/RSS 675.8MB）；稳定性首轮已定（ISS-12：5xx=0/可用性 100%，长尾延迟待根因、MTTR 待 incident 数据） |

**结论**：基线已实质建立——静态指标 + 运行期 API 延迟/QPS 已锁定，且经代码复核修正了 ISS-01（无活跃 N+1）与 ISS-09（限流/熔断/超时均已实现）两处误判。可进入/推进 WF-1，聚焦真正的高收益项：ISS-02 覆盖率、ISS-03 前端首屏、ISS-13 HuggingFace revision pin；资源/SLO 基线待部署态并行补齐。

## 5. 下一步（WF-1 建议顺序）
1. ISS-07 运行 trivy + bandit + npm audit，高危 24-72h 修复。
2. ISS-02 后端测试覆盖率：多轮聚焦补测（已九轮，test_iss02_*.py 22 文件 337 passed；13 个 0% 模块达 72-100% + validation_engine 18 passed/warning_service 9 passed/risk_service_assessment 6 passed/review_service 16 passed/user_data_service 12 passed/model_training 9 passed/pdf_report 16 passed/scheduler 7 passed + celery包装胶水 30 passed + monitoring 内实现 54 passed（silence/dedup 100%、alerting 95.7%、escalation 87.3%、am_sync 84.6%、dedup_lock 63.5%）纯逻辑加固；目标 70-85%，整体受 20054 语句稀释实测 29%，numpy 链模块覆盖%待稳定 CI，pass/fail 本地已稳定）。下一步重点 稳定 CI 全量覆盖率复测（真实覆盖%）+ ml/* numpy 链模块（需 CI 补测，ROI 递减）。
3. ISS-01 为高频关系查询加 `selectinload`，复测 DB 耗时。
4. ~~ISS-03 前端首屏：拆包/懒加载/资源压缩，Lighthouse 复测。~~ 【已实质完成】重测确认原 53 为过期基线；当前移动端 83 / 桌面端 99，zrender 拆包已落地；余下至 90 需非-EP 登录页或 SSR（WF-2/WF-3）。
5. ~~ISS-04 前端 SEO：robots.txt + favicon 修复，Lighthouse 复测。~~ 【已修复】原 SEO 54 为 2026-07-11 旧构建（过期基线，当时 is-crawlable/robots-txt/errors-in-console 均失败）；当前构建重测 91，补 public/robots.txt + index.html 显式 favicon 后升至移动/桌面 100。errors-in-console 残留的 1 个 ReferenceError（Element Plus manualChunks 循环依赖 TDZ）已由 WF-2/ISS-15 修复（vite.config 将 ep-form-advanced/ep-utility 合并回 element-plus 主 chunk，Lighthouse 实测 errors-in-console score=1、best-practices 100，详见 ISS-15-前端A11Y与EP-TDZ治理报告.md）。
6. ~~ISS-14 ws.py pubsub 崩溃重启循环：专用 pubsub 客户端 socket_timeout=None。~~ 【已修复】root cause 为 _pubsub_loop 复用共享 Redis 客户端（其 socket_timeout=2）→ 空闲频道阻塞读超时 → TimeoutError 崩溃重启循环（实测 _uvicorn.log 956 次 "pubsub loop crashed"）；改用专用 pubsub 客户端（get_redis_pubsub_client，独立连接池 + socket_timeout=None + socket_keepalive + health_check_interval=30 + retry_on_timeout），17 passed + 重启实例 35s 实测 0 崩溃/0 TimeoutError。详见 ISS-14-ws-pubsub崩溃循环报告.md。
7. ISS-09 补齐关键接口限流/熔断/超时。
8. ISS-05/06 搭建压测与 APM，锁定运行期基线。
9. ~~ISS-15 前端结构治理(WF-2)：A11Y <main> landmark + EP manualChunks TDZ。~~ 【已修复】App.vue 用 <main> 包裹 router-view（A11Y 94→100，landmark-one-main score=1）+ vite.config 将 ep-form-advanced/ep-utility 合并回 element-plus 主 chunk 消除 EP TDZ（best-practices 96→100，errors-in-console 0 错误）；Lighthouse 移动/桌面 A11Y/BP 均 100。详见 ISS-15-前端A11Y与EP-TDZ治理报告.md。

> 推进原则：先数据后决策、先高收益低成本、每阶段可量化、伴随监控与回滚。任一线上变更经 `sys-release-governance` 灰度+回滚。

## 6. 收尾状态（2026-07-16 更新）

ISS-02 结构性补测已完成 **9 轮、22 个 `test_iss02_*.py`、337 聚焦用例全绿**；celery 包装任务重试/错误处理胶水逻辑已补（第八轮 30 passed）。`backend/scripts/coverage_ci.sh` 已就绪，供稳定 CI 全量覆盖率复测使用。

**已确认完成的 P1/P2 代码项**：
- ISS-01 N+1：复核无活跃 N+1（`get_reviews` 仅序列化标量列）。
- ISS-03 前端首屏：移动 83 / 桌面 99，zrender 拆包落地。
- ISS-06 资源基线：CPU 15.6% / RSS 675.8MB。
- ISS-09 限流：已落地（`app/core/rate_limit.py`，slowapi + 代理感知真实 IP 防 X-Forwarded-For 伪造 + 生产 Redis 后端 + 20+ 端点 `@limiter.limit`），经代码复核确认。
- ISS-12 SLO：5xx=0 / 可用性 100%（长尾延迟待根因、MTTR 待 incident 数据）。
- ISS-13 HuggingFace revision pin：Hub 下载路径已 `revision=DEFAULT_BERT_REVISION` 固定（`config.py:313` B615 供应链说明）；本地路径加载无需 revision。
- ISS-04 SEO=100、ISS-07 漏洞扫描 117、ISS-10 any 治理无活跃风险、ISS-14 ws pubsub 崩溃循环修复、ISS-15 前端结构治理均已完成。

**聚焦套件在 coverage 插桩下的真实验证**：排除 numpy 链模块（`validation_engine` / `risk_service_assessment`，其导入触发本地 Windows coverage SIGSEGV）后，其余 22 文件 / 337 用例在 `--cov=app` 下稳定执行、0 失败，全量 `app` 行覆盖 **33.2%**（仅跑聚焦用例时；完整覆盖% 需稳定 CI 跑全量套件）。

**剩余外部依赖项（非本地代码任务，需流水线 / 运行态）**：
1. ISS-02 / ISS-08 绝对覆盖% 与分支覆盖：本地 Windows 下 numpy 链模块 coverage 插桩确定性 SIGSEGV（C-tracer 与 sysmon 均复现，原生库冲突，非代码缺陷），在 Linux CI 不触发。**CI 流水线已就绪**：`.github/workflows/coverage.yml` 已在 `ubuntu-latest`（postgres:15 + redis:7 服务）运行 `pytest tests/ --cov=app --cov-fail-under=50`（渐进门禁 50→60→70→85）并上传 Codecov，每次 push/PR 自动测量真实 line/branch 数；`backend/scripts/coverage_ci.sh` 为等价本地别名。当前无需额外搭建，覆盖% 将随下次 CI 运行产出。
2. ISS-05 压测基线：**已采集实测基线**（httpx 单worker/20并发，per-endpoint p50/p95/p99/QPS 见 KPI-基线.json：reviews 鉴权墙 p95=46.14ms/p99=49.91ms/501.6QPS、health_ready 571.9QPS、5xx=0%），此项已完成。补充交付零依赖复测脚本 `backend/scripts/loadtest.py`（纯标准库 urllib+threading，输出 QPS/延迟分位数 p50-p99/错误率/状态码分布，无需 k6/locust），已就地冒烟验证有效（对运行态前端 8 并发 3s 实测 2201 QPS、p90-p99=16ms、0 错误），供多 worker/生产态/Redis 后端下的回归复测。若需重跑：`docker run -d -p 6379:6379 redis:7-alpine` + `uvicorn app.main:app --port 8000`（读 `backend/.env`）+ `python scripts/loadtest.py --url http://127.0.0.1:8000/health -c 50 -d 30`（当前本机 redis 无二进制、Docker daemon 未运行，故就地重跑受阻，不影响既有基线）。
3. ISS-11 延迟导入债（P3）：6 处已知豁免（避免循环导入 / numpy 在导入期加载），按设计暂留，ROI 低。
