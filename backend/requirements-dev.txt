# Development and Testing Dependencies
# Install: pip install -r requirements-dev.txt

# Testing Framework
pytest>=8.3.0,<10.0.0
pytest-asyncio>=0.25.0
pytest-cov>=6.0.0
pytest-timeout>=2.3.0
httpx>=0.27.0  # For FastAPI TestClient

# Version compatibility checks
packaging>=24.0  # For sklearn/model version parsing

# Contract Testing
schemathesis>=4.16.0,<5.0.0
hypothesis>=6.152.0
# tests/contract/conftest.py 直接 import starlette_testclient (requests 风格的 TestClient,
# 供 Schemathesis RequestsTransport 复用 session)。它曾是 schemathesis 的传递依赖,
# 但 schemathesis 新版已移除该依赖, 必须在此显式声明, 否则 CI 报 ModuleNotFoundError。
starlette-testclient>=0.4.1

# E2E Testing (optional, mainly for frontend)
# playwright>=1.42.0  # Installed via npm in frontend

# Code Quality
mypy>=1.9.0
ruff>=0.4.0
black>=24.0.0

# Security Testing
bandit>=1.7.8

# SCA (Software Composition Analysis) - SEC-A 依赖项漏洞扫描
# CI 中通过 dependency-scan.yml 调用, HIGH/CRITICAL 漏洞阻塞 PR 合并
pip-audit>=2.7.0
safety>=3.2.0

# Documentation
mkdocs>=1.5.0
mkdocs-material>=9.5.0

# Load Testing
locust>=2.20.0,<3.0.0

# MAINT-P2-003: import-linter (lint-imports) 供 test_importlinter_config 执行契约检查
import-linter>=2.13.0
